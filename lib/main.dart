import 'package:flutter/material.dart';

void main() {
  runApp(MaterialApp(
      home: Scaffold(
          appBar: AppBar(title: Text("Hello world")),
          body: Align(alignment: Alignment(0,0), child: Text("Hello world")))));
}
